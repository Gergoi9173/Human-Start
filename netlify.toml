[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"
  functions = "../netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA fallback - must come AFTER the functions redirect
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
